#include<bits/stdc++.h>
using namespace std;
double myPow(double x, int n) {
        double res =pow(x,n);
        	cout << fixed << setprecision(2) << res << endl;
}
int main(){
    double x=2.00000,n=-2;
    myPow(x,n);
    cout<<endl;
}
